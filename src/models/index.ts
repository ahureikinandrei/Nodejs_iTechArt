import addressModel from './address.model';
import prodModel from './prods.models';
import personalInfoModel from './personalInfo.models';
import userModel from './user.model';

export const UserModel = userModel;
export const PersonalInfoModel = personalInfoModel;
export const ProdModel = prodModel;
export const AddressModel = addressModel;
